function concatenateNames(first, second, del){
let firstSolution = first + del + second;
console.log(firstSolution);

//let secondSolution = `${first}${del}${second}`

}

concatenateNames('John','Smith','->')